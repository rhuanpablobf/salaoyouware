// Este arquivo não é mais necessário para o MongoDB
// Mantido apenas para referência, mas não é usado na versão atual

module.exports = { 
  initDB: async () => {
    console.log('Usando MongoDB - inicialização automática de models');
    return Promise.resolve();
  }
};